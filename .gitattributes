# Normalize line endings: store text as LF in the repo regardless of the OS
# the files were authored on. Keeps the Magento module consistent on checkout.
* text=auto eol=lf

# Treat common binaries as binary (no EOL conversion).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.zip binary
